import React from 'react';
import LoginContainer from 'containers/LoginContainer';

function loginPage() {
  return (
    <React.Fragment>
      <LoginContainer />
    </React.Fragment>
  );
}

export default loginPage;
